# Python dependencies for the recognition pipeline, API, and evaluation workflow.
pillow>=10
requests>=2.31
numpy>=1.26
pandas>=2.0
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9

# CLIP-based recognition stack
# Install a PyTorch build that matches your platform and CUDA support.
torch>=2.2
open-clip-torch>=2.24
